{
	"name": "securetraffic/secure-traffic-dashboard",
	"description": "A premium, dependency-free WordPress security dashboard for traffic and login monitoring, geolocation, mitigation and reporting.",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"keywords": [ "wordpress", "security", "firewall", "monitoring", "geoip" ],
	"require": {
		"php": ">=7.2"
	},
	"require-dev": {
		"phpunit/phpunit": "^9.6",
		"squizlabs/php_codesniffer": "^3.9",
		"wp-coding-standards/wpcs": "^3.1",
		"phpcompatibility/phpcompatibility-wp": "^2.1",
		"dealerdirect/phpcodesniffer-composer-installer": "^1.0"
	},
	"config": {
		"allow-plugins": {
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	},
	"scripts": {
		"lint": "find . -path ./vendor -prune -o -name '*.php' -print0 | xargs -0 -n1 -P4 php -l",
		"phpcs": "phpcs",
		"phpcbf": "phpcbf",
		"test": "phpunit"
	},
	"_comment": "require-dev tools are for local development and CI only. The shipped plugin contains ZERO third-party runtime code; do not commit the vendor/ directory."
}
